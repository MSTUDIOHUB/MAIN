# MAIN Agent Rules

你正在维护的是一个桌面 AI 编程助手，而不是单纯的聊天界面。

## 产品目标
- 先理解用户真正想达成的结果，再决定是聊天、直接执行，还是进入计划流程。
- 尽量提高复杂任务的 Plan → Execute 闭环完成率。
- 把执行过程、计划文档、Diff、终端输出清晰暴露给用户。
- 明确区分三类用户意图：
  - 只要方案
  - 先方案后执行
  - 直接执行

## 模式约束

### Chat
- 主要用于分析、解释、澄清需求、头脑风暴。
- 不要为了“看起来积极”而强行进入写入执行。

### Fast
- 面向明确、可直接落地的任务。
- 输出应聚焦实施与验证，不要额外制造复杂计划。

### Plan
- 面向复杂、多阶段、多文件、高风险任务。
- 先产出规格文件，再等待用户批准执行。
- 如果用户当前只想保留方案，不要强推执行；应优先保留可导出的计划文档。
- 当当前角色偏向数据分析、报表设计、研究总结时，Plan 仍然可用，但应产出“分析计划”而不是默认的代码工程计划。
- 数据分析场景下，`requirements.md` / `design.md` / `tasks.md` 应分别对应分析目标与口径、分析方法与报表结构、查询与验证及出报告步骤。

## 意图理解优先级
在进入规划或执行前，优先识别：
1. 用户最终目标是什么；
2. 用户是要“方案”还是要“改代码”；
3. 成功标准是什么；
4. 哪些约束不能违反；
5. 当前需求里哪些内容只是手段，哪些才是目标。

如果能合理推断，就直接采用最合理假设并继续；
如果存在高风险歧义，再用最小问题量澄清。

如果工作区存在 `.MAIN/templates/intent/` 下的意图模板，优先沿用其字段顺序判断目标、模式、成功标准与约束。

## 实施原则
- 先明确当前关键假设、成功判定与主要约束；如果存在多个会明显影响方案或改动范围的解读，应先点出分歧与取舍。
- 能合理推断时直接继续；只有在高风险歧义会导致返工、误改或错误执行时，才用最小问题量澄清。
- 优先选择“最简单且足够解决当前问题”的方案；不要为单次需求预埋抽象、配置化或未来功能。
- 如果存在更直接、更低风险的做法，应主动采用并说明理由，而不是默认走更复杂的实现。

## 变更边界
- 坚持外科手术式修改：只改与当前需求直接相关的文件、逻辑与文档。
- 不顺手重构、格式化或“优化”无关代码；发现无关问题可以提示，但不要借机扩大改动范围。
- 只清理因本次改动直接产生的无用 import、变量、函数或注释；不要主动删除原本就存在但与本次需求无关的遗留代码。

## Plan 执行完成定义
- `tasks.md` 是计划执行进度的事实来源。
- 完成某项任务后，应同步把对应 checkbox 更新为 `[x]`。
- 在 `tasks.md` 仍有未完成项时，不要把任务宣告为完成。
- 只有当以下条件满足时，Plan 才算真正完成：
  - 代码修改或文档产出已经落地；
  - 没有剩余未完成的计划任务；
  - 最终回复明确说明完成内容、验证结果、残留风险。

## 执行纪律
- 审批通过后的写操作只能执行一次，避免重复写入、重复 replace、重复命令执行。
- 不要把一次普通写文件成功误判成“已完成一个计划任务”。
- 如果执行被阻塞，要明确指出 blocker，而不是静默停止。
- 实施前应先把任务转换成可验证目标；Bug 修复优先先建立复现/验证，再执行修改。
- 最终回复必须交代本次是否已验证、如何验证、还有哪些残留风险或未覆盖项。

## 文档与预览
- `requirements.md` / `design.md` / `tasks.md` / `bugfix.md` 默认位于 `.MAIN/plans/`。
- 生成这些文档时，优先遵循 `.MAIN/templates/plan/` 下对应模板的章节结构。
- 这些文件是临时工作区文档；如果用户想保留方案，应支持导出到普通路径。
- 右侧 Plan 面板应优先展示“当前方案、任务进度、是否可执行”，而不是让用户猜状态。

## Hook 协作
- 如果工作区存在 `.MAIN/hooks.json`，应将其视为执行期护栏；
- Hook 返回的补充上下文不是装饰信息，而是必须纳入后续决策的执行证据；
- 对于完成检查类 Hook，在它仍提示有未完成任务时，不要口头宣布已完成。
