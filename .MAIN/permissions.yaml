shell:
  allow:
    - ls
    - rg
    - cargo check
    - cargo test
    - npm run build
    - npm run lint
    - main eval
    - cargo run --bin main -- eval
    - git status
    - git diff
    - git ls-files
    - node scripts/plan_completion_check.mjs

  deny:
    - sudo
    - rm -rf /
