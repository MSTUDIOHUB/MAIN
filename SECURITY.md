# 安全政策

## 支持范围

MAIN 当前处于快速迭代阶段。安全修复会优先进入最新版本；旧版本不保证单独回补。请先确认问题能在默认分支或最新公开版本中复现。

## 私密报告漏洞

请不要为未修复漏洞创建公开 Issue，也不要在日志、截图或复现仓库中放入真实密钥和个人数据。

优先通过本仓库 Security 页面中的 Private Vulnerability Reporting 提交报告。如果该入口尚未启用，请通过 `MSTUDIOHUB` GitHub 个人资料提供的私密联系方式联系维护者；若没有可用渠道，可以创建一个不含漏洞细节的公开 Issue，请求建立私密联系。

报告中请尽量包含：

- 受影响版本或 commit；
- 影响范围和潜在后果；
- 最小复现步骤或测试夹具；
- 已确认的权限、平台和 Provider 条件；
- 建议修复（可选）。

维护者会确认收到报告、评估影响，并在安全可行时协调修复与披露。请在修复发布前给予合理处理时间。

## 安全边界说明

MAIN 会在自身的文件 API、Shell、工作目录、重定向、网络代理、审批和进程管理边界中执行校验，但它不是操作系统级沙箱。一个已经获准启动的外部程序仍可能按照自身参数或配置访问其他资源。

请始终：

- 在不重要的测试工作区中验证新模型和新工具；
- 审查文件写入、Shell、网络、MCP 和浏览器操作；
- 使用最小权限的 API Key，并通过系统或服务提供的安全方式保存；
- 不要批准来源或影响范围不明的可执行程序；
- 在处理敏感代码时关闭不需要的远程 Provider 和外部工具。

更详细的实现边界见 [docs/TRUSTED_EXECUTION.md](docs/TRUSTED_EXECUTION.md)。
