# MAIN Refactor And Game Studio Status

## Status: 2026-07-10

> 2026-07-10 reassessment: Phase 3S 的结构冻结、行为验收和收尾已经完成，
> Phase 3 正式结束。后续不再以 `AgentOrchestrator.execute` 行数或模块数量推动
> 拆分。Phase 4 Game Studio 领域边界与 Phase 5 的受控 UI/e2e 切片也已完成。
> `ChatArea` 和 `SettingsModal` 经评估后不做低收益整体拆分。当前权威计划见
> `08_MAIN_REFACTOR_REASSESSMENT.md`；下方详细列表保留为实施历史。

This note records the first behavior-equivalent refactor slice for the MAIN architecture and Game Studio plan.

## Stabilized Boundaries

- Pure app/session/task DTOs now live outside the Zustand monolith:
  - `src/lib/appTypes.ts`
  - `src/lib/appConfig.ts`
  - `src/lib/sessionTypes.ts`
  - `src/lib/taskTypes.ts`
  - `src/lib/remoteContextTypes.ts`
  - `src/lib/thoughtCompaction.ts`
- `src/lib/orchestrator/workflowEngine.ts` no longer imports from `src/store/useAppStore.ts`; store-only helpers are injected through `WorkflowEngineStoreHelpers`.
- `src/lib/submit/turnSubmission.ts` owns the first broader pure submit-pipeline slice: submit input envelope construction, pending-review abort decisions, reply-option exact matching, previous-turn reuse, plan hydration signals, intent shortcut parsing, intent-confirmation pending decision construction, initial effective intent/summary/directive resolution, runtime/display intent selection, execution consent, plan-state reset decisions, send busy/queue/pending-review gate decisions, session bootstrap/run metadata decisions, session bootstrap effect patch construction, turn title/session title seed decisions, semantic metadata request decisions, blocking preflight request/action decisions, local Game Studio slash turn patch construction, visible-turn run-state patch construction, launch harness marker draft construction, local execution-approval decisions, blocking-preflight result interpretation, preflight staleness/resume-option decisions, Game Studio mode-switch decisions, visible-turn patch construction, option archival, and operation proposal patching.
- `src/store/submitRuntimeFacade.ts` owns the store-level runtime facade, pre-run session patcher, and elapsed-timer lease for submission runs. This keeps active/background session patching and timer cleanup out of the `sendMessage` body without moving `AbortController` or session runtime ownership into pure submit logic.
- `src/store/submitSessionRuntimeController.ts` owns submit-run scoped state wiring: decorated conversation-turn callbacks, plan task/artifact runtime updates, plan panel opening, current intent lookup, and active/background runtime callback routing.
- `src/store/submitPreflightExecutor.ts` owns blocking intent-preflight effect startup and execution: running the injected preflight request, checking latest composer/session staleness, applying pending decisions through the pre-run session patcher, skipping inactive async resumes, or resuming the original submit with preflight metadata.
- `src/store/submitPendingReviewTransition.ts` owns pending-review interruption effects: aborting the current controller, rejecting the pending review promise, clearing review/tool state, and marking the interrupted source turn as `stopped_no_action` while exact approval reply options stay on the approval path.
- `src/store/submitPlanStateReset.ts` owns submit-time approved-plan runtime reset effects for new requests when the pure runtime decision says plan state should not be preserved.
- `src/store/submitSendGateEffects.ts` owns send-gate effect execution: busy/hidden allowance logging, empty-input blocking, queueing visible submissions with context snapshots, pending-review approval bypass, and stuck running/pending-review reset while the pure decision remains in `src/lib/submit/turnSubmission.ts`.
- `src/store/submitIntentRouting.ts` owns submit-time intent-routing effects: initial effective-intent wiring, same-draft pending-decision suppression, reused-turn execution escalation, plan approval/resume control actions, local execution-approval prompts, and blocking preflight startup while pure intent decisions remain in `src/lib/submit/turnSubmission.ts`.
- `src/store/submitPlanHydration.ts` owns auto plan hydration effect execution: hydrating existing `.MAIN/plans` artifacts, applying plan state, guarding inactive async resumes, and resubmitting with `skipAutoPlanHydration`.
- `src/store/submitPlanExecutionResume.ts` owns the `resume_plan_execution` control effect: clearing pre-run composer state, hydrating or reusing approved plan artifacts, restoring execution plan state, building the trusted resume prompt, and dispatching the hidden execution turn.
- `src/store/submitAttachmentContext.ts` owns submit-time attachment context construction: external attachment ingest, @ mention/file dedupe, text/document/tabular preview formatting, and failed context-item marking callbacks.
- `src/store/submitPromptContext.ts` owns submit-time prompt augmentation: PLAN mode guardrails, PLAN continuation prompts, unfinished-turn continuation prompts, operation-approval continuation prompts, and turn-intake context prefixing.
- `src/store/submitApprovedPlanExecution.ts` owns approved-plan execution prompt construction, runtime task derivation/normalization, command execution hints, and requested root markdown deliverable detection.
- `src/store/submitSessionBootstrap.ts` owns submit-session bootstrap side effects: auto-session patch application and run-session updated-at/active touch.
- `src/store/submitTurnDraft.ts` owns submit-time turn draft preparation: deterministic turn id selection, UI display turn routing, turn input context signals, user context item construction, active session lookup, and local title/seed decision inputs.
- `src/store/submitTitleEffects.ts` owns visible-turn session title seeding and semantic metadata callback handling, including stale callback guards and non-overwrite behavior for manual/semantic session titles.
- `src/store/submitVisibleTurn.ts` owns ordinary visible-turn append, reply-option archival logging, run-state patch application, and context-item failed marking.
- `src/store/submitRunLease.ts` owns agent user-message construction, abort-controller lease creation, goal-start handoff, and harness run marker initialization for a submit run.
- `src/store/submitWorkflowContext.ts` owns initial `WorkflowContext` construction and mutable stream-state defaults for the workflow engine handoff.
- `src/store/submitStreamingUi.ts` owns understanding-progress emission and streaming thought/agent block updates for submit runs.
- `src/store/submitWorkflowEngineRunner.ts` owns workflow-engine helper callback wiring and engine launch for submit runs.
- `src/store/submitAsyncWorkflowRun.ts` owns the async post-submit workflow launch phase: attachment context, prompt augmentation, Game Studio preparation, composer context clearing, run lease creation, workspace tree loading, `WorkflowContext` construction, streaming UI startup, and workflow-engine launch. It keeps a phase-runner test seam so focused node tests can validate ordering and early returns without starting the real engine.
- `src/lib/gameStudio/GameStudioRuntimeService.ts` is the first Game Studio domain facade for initialization, engine configuration, slash command resolution, turn envelope construction, and mode-switch decisions.
- `src/store/gameStudioTurnPreparation.ts` owns store-adjacent Game Studio turn setup: explicit `/setup-engine`, workspace engine auto-configuration, required workflow pack initialization, and turn envelope construction. It depends on an injected runtime service so node tests do not load Vite-only workflow-pack imports.
- `src/store/submitGameStudioPreparation.ts` owns Game Studio preparation result application for submit runs: workspace tree invalidation, runtime patch application, workspace content-version bumps, and failure cleanup that appends the visible system error block and clears active run state.
- `src/store/gameStudioLocalSlashSubmission.ts` owns Game Studio local slash dispatch for `/agent`, `/auto`, and local-fast workflow markdown.
- `src/store/gameStudioLocalSlashBridge.ts` owns Game Studio local slash transcript append, runtime-event emission, and session snapshot persistence.

## Compatibility Shims Kept

- `src/store/useAppStore.ts` still re-exports several moved types and helpers so existing UI/tests do not need a broad migration in the same slice.
- `useAppStore.sendMessage` still owns high-level submission sequencing and async turn preparation. The new `buildSubmitInputEnvelope`, `buildSubmitPipelineDecision`, `buildSubmitIntentConfirmationPendingDecision`, `resolveSubmitEffectiveIntentDecision`, `resolveSubmitRuntimeDecision`, `resolveSubmitSendGateDecision`, `buildSubmitSessionBootstrapDecision`, `buildSubmitSessionBootstrapPatch`, `resolveSubmitTurnTitleDecision`, `resolveSubmitSemanticMetadataDecision`, `buildSubmitBlockingPreflightEffect`, `resolveSubmitPreflightEffectAction`, `buildSubmitLocalStudioTurnPatch`, `buildSubmitRunStatePatch`, `buildSubmitHarnessRunMarkerDraft`, `resolveSubmitPreflightStalenessDecision`, `buildSubmitPreflightResumeOptions`, `buildSubmitVisibleTurnPatch`, `resolveSubmitExecutionApprovalDecision`, `resolveSubmitPreflightResultDecision`, `createSubmitPreRunSessionPatcher`, `createSubmitSessionRuntimeFacade`, `createSubmitSessionRuntimeController`, `prepareSubmitTurnDraft`, `startSubmitElapsedTimer`, `applySubmitPendingReviewTransition`, `applySubmitPlanStateReset`, `applySubmitSendGateEffects`, `resolveAndApplySubmitIntentRouting`, `runSubmitPlanHydrationEffect`, `startSubmitPlanHydrationEffect`, `runSubmitPlanExecutionResumeEffect`, `buildTrustedPlanResumePrompt`, `buildSubmitAttachmentContext`, `prepareAttachedFileForRead`, `buildSubmitPromptContext`, `buildOperationApprovalContinuationPrompt`, `buildApprovedPlanExecutionPrompt`, `ensureApprovedPlanRuntimeTasksForState`, `normalizeApprovedPlanTaskStatuses`, `applySubmitSessionBootstrap`, `applySubmitVisibleTurn`, `runSubmitGameStudioPreparation`, `applySubmitGameStudioPreparationResult`, `prepareGameStudioTurn`, `executeSubmitBlockingPreflight`, `startSubmitBlockingPreflightEffect`, `applySubmitSeedSessionTitle`, `startSubmitSemanticMetadataEffect`, `startSubmitRunLease`, `createSubmitWorkflowContext`, `startSubmitStreamingUi`, `runSubmitWorkflowEngine`, `runSubmitAsyncWorkflowRun`, `startSubmitAsyncWorkflowRun`, `createGameStudioLocalSlashBridge`, and `startGameStudioLocalSlashSubmission` helpers are the intended boundary for continuing Phase 2 without changing the public store hook.
- Game Studio 的旧顶层入口 `gameStudioCatalog.ts`、`gameStudioCommandDocs.ts`、
  `gameStudioOnboarding.ts`、`gameStudioPack.ts`、`gameDevelopmentIntent.ts` 现在都只保留
  一行 re-export。生产代码已迁移到 `src/lib/gameStudio/*` canonical 路径；这些 shim
  仅用于兼容未迁移的外部/历史导入，待发布兼容审计确认没有消费者后再删除。
- `src/lib/e2e.ts` remains the only `src/lib` importer of `useAppStore`. Extracted
  scenario modules receive a minimal store facade from that bridge; the boundary is guarded by
  `tests/node/lib-store-boundary.test.mjs`.

## Phase 4 Completion

- `src/lib/gameStudio/` 现在拥有 catalog、command docs、detection、pack、onboarding
  与 `GameStudioRuntimeService`，共 7 个 TypeScript 文件、2,099 行。
- store、UI 和 `src/lib` 生产代码对五个旧 Game Studio 模块的直接导入为 0；
  `tests/node/game-studio-domain-boundary.test.mjs` 固定该边界并验证 Vite asset glob。
- `GameStudioRuntimeService` 已有直接行为测试，覆盖初始化、引擎配置、`/agent`、
  `/auto`、本地 `/help`、turn envelope 和显式/模糊引擎模式切换。
- installable pack 不再包含 `.claude` 路径、Claude 专属命令工具名或 command
  frontmatter 工具白名单；上游 README、本地设置模板和不受 MAIN 支持的 hook schema
  已移除，MIT 许可证和来源归属保留。
- Game Studio hooks 文档现在只描述 MAIN 支持的四类生命周期事件；命令 guard
  同时覆盖 `run_command` 与 `execute_command`，避免命令工具别名绕过保护。
- onboarding 明确说明：单次代码/普通开发任务使用 MAIN；生命周期、阶段门、持续
  专家角色和长期制作衔接使用 Game Studio。初始化副作用、`/setup-engine`、`/start`
  与 slash hub 的下一步均可见。
- Phase 4 完成时 `Composer.tsx` 为 2,712 行；UI ownership 留给 Phase 5 独立处理。

Phase 4 验收：

- `npm run lint`、`npm run build`、`git diff --check` 通过；
- 全部 1,100 个 node tests 和 `npm run test:workflow-assets` 通过；
- Game Studio onboarding、plan shortcuts、tool group collapse 共 13 个 Playwright
  场景通过；cloud Game Studio execute-reply 场景 1 个通过。

## Phase 5 Completion

- `GameStudioOnboardingPanel` owns onboarding presentation and consumes all bilingual workflow
  copy from `src/lib/gameStudio/onboarding.ts`; initialization/removal and workspace visibility
  state remain in `Composer`.
- `GameStudioSlashMenu` owns only menu rendering and selection events. Slash parsing, filtering,
  keyboard navigation, intent reuse, and command insertion remain in `Composer`.
- `MainModeSwitcher` owns picker presentation and transient hover state. Isolated mode switching
  remains in the store through `switchMainModeWithIsolation`.
- `Composer.tsx` is now 2,386 lines. This is an outcome, not a target; the three extracted modules
  correspond to independently tested ownership boundaries.
- `ChatArea.tsx` and `SettingsModal.tsx` were evaluated separately. Their large remaining blocks
  cross turn/scroll/Plan/transcript state and Cloud/Local/Context controllers respectively, so a
  broad split was intentionally deferred instead of creating parameter-heavy forwarding shells.
- The first e2e scenario registry now lives at `src/lib/e2e/scenarios/cloudSettings.ts`, covering
  three Cloud Settings scenarios while preserving synchronous `window.__CODELY_E2E__` setup and
  the public bridge API. `src/lib/e2e.ts` is 6,847 lines after the first ownership slice.
- Hardcode audit fixes in the same bounded UI/settings slice: local provider endpoints and Cloud
  protocol endpoints now have single registries; the stale OMLX default is `8000/v1`; Context UI
  no longer claims fixed 70/80/90 percent behavior that does not match forced/runtime trimming.

Phase 5 focused acceptance:

- Game Studio onboarding, plan shortcuts, and tool grouping: 13 Playwright scenarios passed.
- Cloud Settings bridge, provider switching, empty/server-model seeds, and model refresh: 5
  Playwright scenarios passed.
- The new `src/lib` -> Zustand architecture guard and endpoint registries have direct Node tests.

## Phase 6 Integration Result

- Final `npm run build`, `npm run lint`, `npm run test:workflow-assets`, and full-tree
  `git diff --check` passed. The Node suite contains 1,102 passing tests.
- The complete Playwright run was executed: 110 passed, 2 environment-dependent OMLX cases were
  skipped, and 33 failed. Nine failures were stale Cloud OAuth-lab expectations and one was a
  stale MAIN shortcut catalog expectation; focused reruns now show 18 passed and 7 explicitly
  skipped for the disabled OAuth lab.
- The remaining 23 Playwright failures are recorded baseline debt across Cloud tool runtime (6),
  FilePanel (6), transcript/process display (7), Feishu (1), Plan refresh (1), and sessions (2).
  None of those owners were structurally moved in Phase 5, so they are not folded into this
  refactor without a separate behavior investigation.
- Current structural snapshot: `useAppStore.ts` 6,990 lines, `AgentOrchestrator.ts` 542,
  `Composer.tsx` 2,386, and only `src/lib/e2e.ts` imports the Zustand store from `src/lib`.

## Behavior Notes

- Normal MAIN mode can still handle game-development tasks.
- Game Studio remains a workspace-local workflow pack, not a replacement for MAIN general execution.
- Local-fast Game Studio slash help bypasses execution approval and renders command markdown directly in the transcript.
- `game_studio_local_markdown` is visible transcript output and is not folded into process timelines.
- Awaiting-input turns keep effective progress visible without showing a stale running-tool state.

## Phase 2 Boundary Audit

- `useAppStore.sendMessage` is currently 649 lines by TypeScript AST range. The remaining body now mainly sequences existing submit helpers and store effects: input envelope/pipeline decision, intent routing, runtime decision, send gate, session bootstrap, turn draft/materialization, local Game Studio slash handling, title metadata, elapsed timer, and async workflow launch.
- The earlier 300-500 line target is directional. For this slice, the next obvious extraction would mostly wrap many store callbacks and already-named helpers into another parameter-heavy facade, so it is intentionally left in `sendMessage` until a later phase creates a clearer owner.
- The Phase 2 stop point is therefore ownership-based rather than line-count-based: pure decisions and behavior-heavy effects have focused helper/test coverage, while `sendMessage` remains the public hook entrypoint and high-level submit orchestration layer.
- Further reduction should wait for a coherent Phase 3/4 boundary, such as an orchestrator callback contract change or a narrower Game Studio runtime service handoff.

## Phase 3 Progress

- `src/lib/orchestrator/loop/AgentLoopRunner.ts` now owns the outer `executeAgentLoop` runner: creating the `AgentOrchestrator`, wrapping callbacks into structured `AgentLoopOutcome`, preserving awaiting-input pause semantics, handling aborts, and invoking completion guards.
- `src/lib/orchestrator/loop/completionGuards.ts` now owns final outcome guards: non-actionable stop outcome mapping, approved-plan evidence audit, final turn-contract fallback, and execution-evidence-required pauses.
- `src/lib/orchestrator/loop/turnPreparation.ts` owns the turn-preparation boundary: `AgentLoopRuntimeState`, turn event emitter/initial progress logging, runtime/reasoning telemetry, `AgentLoopTurnInputContext` for latest user prompt, turn input signals, Game Studio engine cues, and Unity diagnostic/script-edit routing cues, `createSystemPromptApplier` for turn-contract calculation, system-prompt refresh, tool-protocol card logging, and Game Studio/MCP prompt context wiring, plus startup helpers for resolved instructions, hooks loading/execution, task-targeting intake telemetry, and non-blocking model probing.
- `src/lib/orchestrator/loop/streamInvocation.ts` owns the first stream-invocation boundary: tool-protocol message preparation, max-step/chat-final-synthesis prompt injection, plan watchdog/hard-timeout options, recovery tool-choice selection, LLM request-shape telemetry, response schema policy, initial `fetchLLMStream`, reasoning-dominated no-action pause, native-tool success marking, and local empty-completion context retry trigger.
- `src/lib/orchestrator/loop/streamRecovery.ts` owns the first reactive stream-recovery boundary: initial invocation handoff, local/cloud context retry compaction, emergency tool-call-stripped retries, native-tool provider compatibility fallback, provider-compatibility text retry, transcript fallback, plan-design watchdog pauses, and terminal stream error mapping.
- `src/lib/orchestrator/loop/streamRuntimeState.ts` owns stream-control runtime state: max-step final-text prompt usage, chat final-synthesis activation/reason/prompt-use state, capped max-token overrides, max-output escalation policy, and Plan no-visible-token watchdog notice gating. `AgentOrchestrator.execute` now carries one `streamRuntimeState` object instead of separate stream/final-synthesis locals, while the actual log side effects stay in the main loop.
- `src/lib/orchestrator/loop/recoveryPromptRuntimeState.ts` owns one-shot recovery prompt state: tool-unavailable reprompts, pseudo-tool repair prompts, malformed tool-use recovery, language-mismatch recovery, read-only permission hard recovery, and execute-convergence prompts. Transient prompt flags reset when real tool calls are detected, while execute-convergence state preserves the previous cross-result behavior.
- `src/lib/orchestrator/loop/noToolRuntimeState.ts` owns shared no-tool loop runtime state: consecutive no-tool counters, empty-response counters, reasoning-dominated counters, and the post-write empty-assistant recovery flag. `AgentOrchestrator.execute` now applies no-tool, empty-response, reasoning-only, missing-tool, approved-plan, execute, and tool-result updates through explicit reducers instead of five loose locals.
- `src/lib/orchestrator/loop/loopGuardRuntimeState.ts` owns loop-guard runtime state for repeated-read/no-progress recovery: cross-iteration read counts, no-progress batch tracking, read-file-only streaks, repeated-edit-without-verification targets, repeat-guard signatures, target-progress signatures, and failed tool-call counts. The helper keeps the existing Map/Set mutation semantics but gives `AgentOrchestrator.execute` one explicit owner for read-loop and repeat-loop guard state.
- `src/lib/orchestrator/loop/toolExecutionRuntimeState.ts` owns per-loop tool execution caches: read-only result cache, approved-plan browser validation reuse cache, read-only duplicate skip counts, and the session-scoped file-read state map. `AgentOrchestrator.execute` now passes this cache object into context management, tool-call partitioning, and tool-execution rounds instead of carrying four cache locals.
- `src/lib/orchestrator/loop/evidenceRuntimeState.ts` owns completion-evidence and checkpoint state: the latest successful project write, whether execution evidence has been observed, and the last assistant checkpoint text. The class-level `sawExecutionEvidence` compatibility field remains the public completion-guard bridge, while the loop body now reads helper inputs from one explicit evidence state object.
- `src/lib/orchestrator/loop/toolCallPlanning.ts` owns the first per-iteration tool-surface planning boundary: execute-recovery tool narrowing, adaptive file-read allowance, approved-plan source-edit-first narrowing, approved-plan action/file-read recovery narrowing, plan runtime phase tool filtering, post-convergence plan write-only closure, and the `execute_recovery_tool_scope_applied`, `recovery_loop_summary`, `tool_surface_decision`, and `plan_runtime_tool_scope_applied` telemetry.
- `src/lib/orchestrator/loop/toolCallPartitioning.ts` owns tool-call partitioning before execution: task-targeting gates, repeated-failure blocking, runtime permission planning, read-only cache hits/replay, approved-plan patch-recovery read-cache bypass, browser validation reuse, and read/local/spec/write queue construction.
- `src/lib/orchestrator/loop/toolExecutionRound.ts` owns the first tool-execution round boundary from partitioned queues: concurrent read-only execution, review-gated external local reads, auto-approved spec-file calls, sequential write/review execution, read_file cache storage, narrowed read-window replay, and browser validation reuse cache updates.
- `src/lib/orchestrator/loop/toolResultHistory.ts` owns tool-result history emission: TurnContext execution registration, tool-role history messages, runtime `item.completed` tool-result events, lifecycle state mapping, and PostToolUse hook context injection.
- `src/lib/orchestrator/loop/loopRecovery.ts` owns the first loop-recovery slices after tool execution: no-progress batch tracking, execute/chat read-only recovery, approved-plan cached-read no-progress decisions, cross-iteration read_file loop recovery, blocked read_file recovery reset prompts, strict repeat guard recovery, target-progress loop recovery, execute-convergence prompting, approved-plan read_file repeat-limit pauses, approved-plan repeated browser/read_file pauses, edit-mode read_file repeat-limit recovery prompts/pauses, and repeated-edit-without-validation recovery. The helpers can still trigger execute recovery through the outer activator so same-iteration logs and decisions stay behavior-equivalent after extraction; strict repeat recovery also preserves the read-only shell failure-count side effect that prevents terminal-read bypass loops.
- `src/lib/orchestrator/loop/executeRecoveryRuntime.ts` owns execute-recovery runtime state: forced edit-mode recovery initialization, activation attempt counting, max recovery iteration advancement, clear semantics, blocked read_file recovery counters, repeated-edit validation counters, and the shared max-iteration recovery prompt. `AgentOrchestrator.execute` now carries one `executeRecoveryState` object instead of separate `executeRecoveryMode`, `executeRecoveryReason`, `executeRecoveryAttempts`, and recovery iteration locals, while preserving the existing blocked-read reset behavior that keeps attempt history across non-successful recovery resets.
- `src/lib/orchestrator/loop/maxIterationBoundary.ts` owns loop exhaustion handling after the main iteration loop: approved-plan max-iteration checkpoints, execute max-iteration checkpoints, generic max-iteration safety stops, checkpoint pause notices, and the `max_iterations_checkpoint`, `execute_max_iterations_checkpoint`, and `max_iterations_boundary` telemetry. The helper preserves the previous Plan/Edit/Chat stop semantics, including the generic boundary's final turn-completed event.
- `src/lib/orchestrator/loop/approvedPlanRecoveryActions.ts` owns approved-plan recovery side effects that are reused across stream and no-progress paths: no-progress pause notices, stream-watchdog pauses, and action-only strategy-switch prompts. The strategy-switch helper returns updated recovery state explicitly so `AgentOrchestrator.execute` keeps local state ownership without hiding mutation inside a large closure.
- `src/lib/orchestrator/loop/approvedPlanRecoveryRuntime.ts` owns approved-plan recovery runtime state: no-progress recovery attempts, action-only recovery mode, no-tool recovery file-read allowance, and long-reasoning no-action counters. `AgentOrchestrator.execute` now carries one `approvedPlanRecoveryState` object and applies strategy-switch, no-tool, reasoning-only, tool-result, and plan-handoff updates through explicit reducers instead of four loose locals.
- `src/lib/orchestrator/loop/approvedPlanNoToolRouting.ts` owns the pure approved-plan no-tool route decision before recovery/finalization: trusted-evidence audit construction, missing-task detection, remaining-task suppression, completion-claim rejection, visible-text hiding, and `approved_plan_no_tool_route` eligibility. `AgentOrchestrator.execute` keeps the telemetry side effects while the audit/suppression policy is now testable without running the loop.
- `src/lib/orchestrator/loop/approvedPlanNoToolRecovery.ts` owns the approved-plan no-tool execution recovery boundary: no-tool count updates, long-reasoning no-action strategy switches, cached-read truncation recovery, recovery tool-surface activation, no-tool checkpoint pauses, and `plan_execution_no_tool_reprompt`, `approved_plan_no_tool_recovery_tool_surface`, and `approved_plan_reasoning_length_no_action` telemetry. The helper returns updated recovery counters explicitly and names the local-profile checkpoint limit through `resolveApprovedPlanNoToolCheckpointLimit` instead of leaving the threshold inline in `execute`.
- `src/lib/orchestrator/loop/approvedPlanFinalization.ts` owns approved-plan no-tool finalization after recovery paths decline to act: trusted evidence audit, remaining-task continuation prompts, external/user validation pauses, remaining-plan checkpoint pauses, final completion-gate reprompts, completed-stage transition, and the `plan_execution_validation_boundary`, `remaining_plan_tasks_limit`, `plan_completion_guard_reprompt`, and `plan_evidence_complete` telemetry.
- `src/lib/orchestrator/loop/finalTurnCompletion.ts` owns assistant turn completion after no-tool/final-choice paths: appending assistant history, emitting `item.completed`, preserving reply-options pauses, mapping unapproved Plan final text to `incomplete_plan`, mapping normal final text to `assistant_text_done`, setting idle, and emitting turn completion. This keeps final-turn event ordering in one tested helper instead of duplicating it inside `AgentOrchestrator.execute`, and now receives event identity through `TurnIterationContext`.
- `src/lib/orchestrator/loop/assistantCompletionPhase.ts` owns the post-display assistant completion phase: reply-option pauses, approved-plan no-tool recovery, execute no-tool recovery, unapproved Plan no-tool recovery/materialization, missing-tool recovery, Unity final-console verification prompts, approved-plan finalization, final no-tool assistant completion, and the no-tool/Plan/approved-plan recovery state folds between those handlers. `AgentOrchestrator.execute` now delegates this phase and only applies the returned runtime-state objects plus `continue`/`stopped` control flow.
- `src/lib/orchestrator/loop/turnIterationContext.ts` introduces the first explicit `TurnIterationContext`: per-iteration legacy thread reuse/reset, iteration turn id construction, `ExecutionTurn` creation, and `TurnContext` lifecycle start. `AgentOrchestrator.execute` now keeps a typed `loopThread` field instead of touching `(this as any)._thread`, and passes the iteration context into context management, tool-call partitioning, tool-result history, and final-turn completion so event identity and turn lifecycle state have a shared per-iteration owner.
- `src/lib/orchestrator/loop/assistantResponseProcessing.ts` owns stream-response normalization before recovery/tool routing: provider reasoning history metadata extraction, `stream_done`/low-content diagnostics, hidden reasoning suppression telemetry, TurnContext reasoning summary accumulation, empty-response diagnostics, `normalizeAssistantTurn`, and visible-conclusion policy application.
- `src/lib/orchestrator/loop/assistantStreamPostProcessingPhase.ts` owns the post-stream/pre-display phase orchestration: assistant response processing, reasoning-dominated no-tool recovery, empty-response recovery, tool-call normalization and `tool_protocol_parse_recovered` telemetry, final-text-only tool-call handling, and the no-tool/Plan/approved-plan/recovery-prompt state folds between those handlers. `AgentOrchestrator.execute` now receives a completed stream payload or a `continue`/`stopped` control result from one tested phase boundary.
- `src/lib/orchestrator/loop/assistantDisplayActionPhase.ts` owns the display/action/no-tool recovery phase: assistant display decision, pseudo-tool recovery and required web-search injection side effects, reply-option suppression telemetry, synthetic conclusion suppression, and assistant no-tool recovery handoff. `AgentOrchestrator.execute` now receives the effective tool calls, display artifacts, recovery-prompt state, and read-only activity count from one phase boundary.
- `src/lib/orchestrator/loop/assistantTurnDisplay.ts` owns the pure assistant visible-text/reply-option display decision after response normalization: prose-code-dump compaction, truncated Plan fallback display, read-only permission option suppression/auto-continuation, non-blocking Plan option suppression, approved-plan execution option suppression, unapproved Plan reply-option artifact routing, and final visible text/reply option selection.
- `src/lib/orchestrator/loop/assistantActionRouting.ts` owns the pure assistant action-routing decision after display resolution: pseudo-tool recovery via existing `choosePseudoToolRecovery`, required `web_search` injection gated by `webSearchEnabled`, duplicate web-research suppression from recent tool activity, pseudo-tool placeholder detection, synthetic visible-conclusion suppression, and final user-visible text selection.
- `src/lib/orchestrator/loop/assistantNoToolRecoveryRouting.ts` owns the first assistant no-tool recovery route after action routing: chat final-synthesis activation for length/read-only loops, cloud tool-unavailable claim reprompting, pseudo-tool repair prompting, and pseudo-tool doom-loop stops.
- `src/lib/orchestrator/loop/assistantLanguageRecoveryRouting.ts` owns wrong-language assistant recovery routing: one-shot reprompt decisions, wrong-language text hiding when tool calls are present, exhausted retry detection, and chat final-synthesis activation after a failed language retry. The lower-level language detector remains in `agentRecovery.ts`.
- `src/lib/orchestrator/loop/assistantRecoveryHandling.ts` owns the side-effect boundary for assistant no-tool and language recovery routes: runtime status updates, escalation stream resets, recovery prompt state marking, user recovery prompt injection, pseudo-tool doom-loop stops, and wrong-language visible-text suppression. `AgentOrchestrator.execute` now receives a compact `pass`/`continue`/`stopped` result and an updated recovery state instead of directly coordinating these route side effects.
- `src/lib/orchestrator/loop/assistantOutputRouting.ts` owns pure assistant output route decisions after progress routing: raw tool-protocol stream clearing, checkpoint text tracking, non-blocking Plan choice auto-continuation eligibility, reply-option pause/executable-proposal detection, and hidden-thought-only no-tool stop detection.
- `src/lib/orchestrator/loop/assistantOutputPhase.ts` owns the assistant output/progress presentation phase after display/action routing: approved-plan no-tool suppression telemetry, language recovery handoff, tool progress narration and UI metadata, raw tool-protocol stream clearing, summary/final-text emission, read-only permission auto-continuation, assistant-history serialization, checkpoint evidence folding, reply-option rejection telemetry, and unapproved Plan post-convergence redirects. `AgentOrchestrator.execute` now passes one phase result into assistant completion and tool-call execution instead of directly coordinating presentation, recovery, and history side effects.
- `src/lib/orchestrator/loop/assistantIterationPhase.ts` owns the post-stream/pre-tool assistant iteration orchestration: assistant stream post-processing, display/action routing, Unity MCP no-tool recovery, assistant output/progress presentation, assistant completion/no-tool recovery, and the runtime-state folds between those child phases. `AgentOrchestrator.execute` now delegates one assistant iteration phase and receives only the tool-execution handoff fields: effective tool calls, assistant history text, provider reasoning metadata, final reply-option count, and structured-proposal status.
- `src/lib/orchestrator/loop/finalTextOnlyToolCallHandling.ts` owns the side-effect boundary for final-text-only/max-step or chat-final-synthesis turns where the model still emits tool calls: stream escalation reset, ignored-tool telemetry, unresolved chat repair no-action pause, model-authored final-text completion/history emission, and max-iteration/chat-synthesis no-action stop notices. This keeps the final-synthesis stop semantics testable while reusing `finalTurnCompletion.ts` for the actual assistant-history/event completion path.
- `src/lib/orchestrator/loop/iterationStreamPreparation.ts` owns the per-iteration pre-stream phase orchestration: `startNewTurn`, max-step final-text-only state, execute-recovery iteration advancement/max-recovery reset prompts, per-iteration tool-surface planning, system-prompt application, managed context preparation, runtime guidance injection, assistant message id allocation, max-output escalation lookup, and `iteration_start`/harness-running telemetry. `AgentOrchestrator.execute` now receives a prepared stream request and keeps the following stream invocation/recovery flow readable without directly coordinating tool-surface, context, and guidance setup.
- `src/lib/orchestrator/loop/reasoningNoToolRecovery.ts` owns reasoning-dominated no-tool fallback: unapproved Plan targeted-evidence recovery, reasoning-only blocked pauses, generic reasoning-only retry/pause, approved-plan action-only recovery surface activation, and edit/execute mutation-first recovery activation. It centralizes `plan_reasoning_only_recovery_decision`, `reasoning_dominated_recovery`, `reasoning_dominated_pause`, `approved_plan_reasoning_recovery_tool_surface`, and `plan_reasoning_only_evidence_blocked` telemetry while returning updated counters and recovery flags explicitly.
- `src/lib/orchestrator/loop/emptyResponseRecovery.ts` owns empty-response no-tool fallback: review-ready Plan pauses, malformed Plan `<tool_use>` recovery, chat empty-output stop checkpoints, Plan empty-response closure guard handling, generic empty-output pauses, and post-write verification prompts. It centralizes `plan_review_ready_after_empty_response`, `tool_protocol_parse_failed`, `empty_model_response`, `plan_empty_after_closure_failed`, and `plan_empty_response_checkpoint` telemetry while returning updated empty-response counters and recovery flags explicitly.
- `src/lib/orchestrator/loop/executeNoToolRecovery.ts` owns generic execute no-tool recovery for plain-text completion claims without evidence, replanning text emitted during execute turns, and XML-profile plain-text misses. The helper returns the updated no-tool counter explicitly, centralizes `execute_completion_claim_without_evidence`, `execute_replanning_text_without_evidence`, and `execute_xml_text_without_action` telemetry, and exposes `resolveExecuteNoToolCheckpointLimit` so the local/cloud checkpoint policy is no longer hard-coded in the main loop.
- `src/lib/orchestrator/loop/missingToolNoToolRecovery.ts` owns missing-tool no-tool recovery: promised action without tool calls, hidden-thought-only stops, post-write verification reprompts, and execute read-only no-action checkpoint pauses. It centralizes `missing_tool_reprompt`, `missing_tool_reprompt_limit`, and `execute_read_only_no_action_checkpoint` telemetry while returning updated no-tool and post-write recovery state explicitly.
- `src/lib/orchestrator/loop/planNoToolRecovery.ts` owns unapproved Plan no-tool materialization and recovery: structured proposal materialization, visible plan text materialization, recovery prompt limits, closure-evidence prompts, long-plan refinement, and forced plan continuation. The helper centralizes `plan_structured_proposal_materialized`, `plan_text_materialized`, `plan_recovery_prompt_start`, `plan_refine_long_output`, and `force_plan_continuation_limit` telemetry while returning updated no-tool, recovery-prompt, and closure-evidence state explicitly.
- `src/lib/orchestrator/loop/unityMcpRuntime.ts` owns the Unity MCP runtime boundary: MCP-first tool ordering, the shared forced `read_console`/`set_active_instance` strict-retry tool list, forced `read_console` strict tool exposure fallback, first-iteration no-tool strict retry/fallback decisions, no-tool recovery side effects, forced-console result handling, and `UnityMcpRuntimeState` reducers for fallback activation, no-tool recovery updates, tool-call reset, and post-result state reconciliation. `AgentOrchestrator.execute` now carries one Unity runtime state object and calls `handleUnityMcpNoToolRecovery` instead of owning Unity retry telemetry/prompt appends inline.
- `src/lib/orchestrator/loop/contextManagement.ts` owns the first stream-preparation context boundary: XML/native tool protocol selection, LLM tool exposure for the iteration, execute-recovery context compaction, cached failed-file adaptive context injection, proactive reasoning strain/ephemeral pruning, managed context pack construction, and the `context_pack_built`, `execute_recovery_context_compacted`, and `execute_recovery_context_skipped` telemetry. Reactive context retry and provider compatibility fallback now live in `streamRecovery.ts`; event turn identity and `TurnContext` lifecycle access are supplied through `TurnIterationContext`.
- `src/lib/orchestrator/loop/toolActivityTracking.ts` owns the first execution-evidence/tool-activity boundary: filtering no-op/cached/no-effect tool results out of execution evidence, excluding plan artifact writes from execution evidence, command/browser success checks, recent tool activity summarization, edit-progress classification, and verification-evidence classification.
- `src/lib/orchestrator/loop/toolProgressRouting.ts` owns assistant tool-progress routing before UI emission: unsupported-tool suppression, unapproved Plan draft artifact exceptions, substantive Plan text detection, runtime narration injection eligibility, progress visibility, approved execution text preservation, capsule candidacy, and model-authored/progress attribution. `AgentOrchestrator.execute` still builds the actual narration and emits UI/log side effects.
- `src/lib/orchestrator/loop/toolResultPostProcessing.ts` owns post-tool result state reconciliation: execution-evidence marking, task-targeting evidence capture, Unity final console verification toggles, forced-console fallback prompts, execution digest updates, plan drafting read counters, project-structure phase transitions, `EVIDENCE_RECONCILE` telemetry, approved-plan recovery resets, edit recovery clearing, and unapproved Plan read-only batch classification.
- `src/lib/orchestrator/loop/toolCallExecutionPhase.ts` owns the tool-call execution phase orchestration: resetting no-tool/recovery prompt state after tool calls appear, marking Unity MCP tool-call state, emitting `tool_calls_detected`/`EXECUTE_STEP`, appending assistant tool-call history, partitioning tool calls, executing read/write rounds, running post-tool reconciliation, and folding no-tool, Plan, Unity, evidence, and approved-plan recovery runtime state updates.
- `src/lib/orchestrator/loop/toolResultRecoveryPhase.ts` owns the post-tool-result recovery phase orchestration: Plan quality recovery, no-progress decisions, tool-result history emission, read-file repeat-limit handling, cross-iteration read-loop recovery, Unity MCP fallback prompt injection, repeated-edit validation recovery, approved-plan strategy-switch/pause decisions, unapproved Plan read-only convergence, post-tool Plan artifact review, approved-plan progress updates, strict repeat guard, target-progress guard, execute-convergence prompting, and `post_tool_result_continuation` telemetry.
- `src/lib/orchestrator/loop/toolIterationPhase.ts` owns the high-level post-assistant tool iteration orchestration: executing the tool-call phase, keeping `allResults`/`toolArgsByCallId`/failure signatures inside the tool boundary, passing only the completed execution outputs into tool-result recovery, folding the no-tool/Plan/loop-guard/execute-recovery/recovery-prompt/Unity/evidence/approved-plan runtime states, and returning a single `aborted`/`stopped`/`continue`/`completed` control result to `AgentOrchestrator.execute`.
- `src/lib/orchestrator/loop/toolRegistrySetup.ts` owns the pre-loop tool registry setup boundary: MCP discovery/status telemetry, Game Studio/Unity MCP-first routing decisions, prompt-facing MCP routing, Unity edit tool annotation, web/knowledge tool gating, tool definition construction, capability registry construction, and effective Unity MCP tool-name selection. MCP-first runtime filtering and mutable fallback state transitions now live in `unityMcpRuntime.ts`.
- `src/lib/orchestrator/loop/toolSurfaceRuntime.ts` owns runtime tool-surface wiring after registry setup: effective runtime intent resolution, intent-filtered tool selection, global-chat tool scope reduction, Unity MCP-first per-iteration ordering, Unity fallback state activation, and `global_chat_tool_scope_applied`/`unity_mcp_fallback` telemetry. `AgentOrchestrator.execute` now uses one tool-surface runtime object instead of carrying these provider/tool-scope closures inline.
- `src/lib/orchestrator/loop/planConvergence.ts` owns the unapproved Plan read-only convergence boundary and the post-convergence unsupported-tool redirect boundary: read-only convergence counter updates, evidence-readiness assessment, threshold prompt injection, Plan runtime phase changes, drafting recovery read injection, suppressed-tool targeted-evidence recovery, forced-write recovery after exhausted reads, auto-scaffold escalation after repeated blocked reads, and the `plan_readonly_convergence_threshold`, `plan_post_convergence_tool_redirect`, `plan_drafting_recovery_read_injected`, `plan_suppressed_tool_recovery_decision`, and `plan_suppressed_tool_forced_write_injected` telemetry. During extraction the old second-pause branch was confirmed unreachable because `shouldTriggerPlanReadOnlyConvergence` already returns false after the convergence prompt has been used, so the helper preserves current behavior without carrying dead code forward. The runtime redirect policy was renamed to `shouldRedirectPlanRuntimeToolsAfterReadOnlyConvergence` so it no longer collides with the tool-surface policy in `planReadOnlyConvergence.ts`.
- `src/lib/orchestrator/loop/planQualityRecovery.ts` owns post-tool unapproved Plan quality recovery: quality-gate internal feedback handling, targeted-evidence phase transitions, auto-scaffold prompts, model-authored closure-evidence prompts, evidence-recovery pass completion/block prompts, and the `plan_quality_recovery_action` and `plan_quality_gate_recovery_decision` telemetry. The helper snapshots the incoming `needs_evidence` phase before quality recovery mutates runtime phase, preserving the previous evidence-recovery ordering.
- `src/lib/orchestrator/loop/planRuntimeState.ts` owns unapproved Plan loop runtime state: phase, quality-reject counters, missing-section evidence, reasoning/evidence recovery passes, auto-scaffold and closure-evidence flags, drafting recovery reads, read-only convergence counters, Plan tool-activity detection, recovery prompt flags, closure prompt guards, and post-convergence redirect counts. `AgentOrchestrator.execute` now carries one `planRuntimeState` object and applies reducer-style updates from reasoning, no-tool, post-convergence, tool-result, quality, and read-only convergence helpers instead of loose Plan runtime locals.
- `src/lib/orchestrator/loop/planReviewRuntime.ts` owns the Plan review/closure boundary: waiting for approval, pausing on reviewable plan artifacts, resetting approved-plan handoff recovery state, building the approved-plan continuation prompt, and closing unapproved Plan loops from accumulated evidence. `AgentOrchestrator.execute` now wires these handlers with explicit state getters/setters instead of carrying the review/closure implementation inline.
- `src/lib/orchestrator/loop/loopControlRuntime.ts` owns loop-control and progress wiring: max-iteration resolution, Plan execution progress updates, approved-plan no-progress/stream-watchdog pause handlers, approved-plan strategy-switch recovery state updates, Plan stream watchdog option state, max-output escalation policy entry, and loop-start telemetry. `AgentOrchestrator.execute` now creates one `loopControl` object instead of carrying these progress/action wrappers inline.
- `src/lib/orchestrator/loop/loopRuntimeActions.ts` owns mutable loop action closures shared across phases: execute-recovery activation/clearing, chat final-synthesis activation, cross-iteration read tracking reset on recovery clear, Plan runtime phase updates, and the paired `execute_recovery_activated`, `execute_recovery_cleared`, `chat_final_synthesis_activated`, and `plan_runtime_phase_changed` telemetry. `AgentOrchestrator.execute` now passes these stable action callbacks to phase helpers instead of directly owning the reducer/telemetry sequence.
- `src/lib/orchestrator/loop/loopMutableState.ts` owns the retained loop-level mutable runtime state object: iteration, no-tool, stream, recovery-prompt, Plan, evidence, approved-plan recovery, execute-recovery, loop-guard, Unity MCP, recent activity, attempted Plan write targets, and tool-execution caches. It also owns common phase-result folds for stream preparation, assistant iteration, tool iteration, execution-evidence marking, and chat final-synthesis prompt marking, so `AgentOrchestrator.execute` no longer initializes or manually reassigns these runtime state objects one by one.
- The public export remains compatible through `src/lib/orchestrator.ts`; `workflowEngine` can keep importing `executeAgentLoop` from the existing orchestrator facade.
- `AgentOrchestrator.ts` is currently 542 lines by `wc -l`, with `execute` 501 lines by TypeScript AST range. The retained body now reads as high-level preparation, iteration, assistant, tool, and max-iteration orchestration. Phase 3S removed unused placeholder methods, froze the loop at 62 modules, and completed behavioral acceptance without another structural split.

## Historical Pending Phases

The inventory below records the previous continuation plan. It is superseded by
`08_MAIN_REFACTOR_REASSESSMENT.md` wherever it suggests further Phase 3 extraction.

- Phase 2 has a test-covered pure-decision/effect-plan boundary for submit input envelope construction, reply-option reuse, pending-review interruption effects, plan hydration decisions and auto hydration execution, trusted plan execution resume, submit attachment context construction, submit prompt augmentation, approved-plan execution prompt/runtime task construction, intent shortcuts, intent-confirmation pending decision construction, effective intent resolution, auto-approve execution forcing, runtime/display intent selection, execution consent, plan-state reset decisions and reset effects, send busy/queue/pending-review gate decisions and effect execution, submit intent-routing effect execution, pre-run session patch routing, session bootstrap/run metadata, temporary-session bootstrap patching and store-side application, submit scoped runtime callbacks, turn draft construction, turn title/session title seed decisions, semantic metadata request planning and callback execution, blocking preflight request/action planning and execution startup, local Game Studio slash turn patch construction, dispatch, transcript persistence, ordinary visible-turn append/run-state application, launch harness marker draft construction and run lease creation, async workflow-run launch orchestration, initial WorkflowContext construction, streaming UI ownership, workflow-engine callback wiring, store runtime facade/timer ownership, preflight stale/resume handling, visible-turn patch construction, local execution-approval prompts, blocking-preflight outcomes, Game Studio mode switching, Game Studio setup/envelope preparation, and Game Studio preparation result application/failure cleanup. `sendMessage` is currently 649 lines by TypeScript AST range; remaining Phase 2 work should keep moving coherent decisions/effects out of `useAppStore.sendMessage` only when the extracted boundary improves readability, testing, or ownership. The earlier 300-500 line target is directional, not a hard gate; do not split stable code just to satisfy a line count.
- Phase 3 still needs `AgentOrchestrator.execute` split into broader recovery phases. Runtime state, turn-start preparation, loop-control/progress wiring, system-prompt application, pre-loop tool registry setup, runtime tool-surface wiring, mutable loop runtime actions, loop mutable state, stream-preparation context management, per-iteration stream request preparation, initial stream invocation, stream-control runtime state, recovery prompt runtime state, no-tool runtime state, loop-guard runtime state, tool-execution runtime state, evidence runtime state, assistant response processing, assistant stream post-processing phase, assistant display/action phase, assistant output/progress presentation phase, assistant iteration phase, assistant display/reply-option decision, assistant action-routing decision, assistant no-tool recovery routing/handling, assistant language recovery routing/handling, assistant output routing, final-text-only tool-call handling, approved-plan no-tool routing, tool-progress routing, reactive stream recovery, per-iteration lifecycle context, execute-recovery runtime state, approved-plan recovery runtime state, Plan runtime state, Plan review/closure runtime, per-iteration tool-surface planning, tool-call partitioning, tool-call execution phase, tool-result recovery phase, tool-iteration phase, tool-execution round dispatch, tool-result history emission, tool-result post-processing/state reset, no-progress/read-file/strict-repeat/target-progress/execute-convergence/repeated-edit recovery slices, max-iteration boundary handling, approved-plan recovery actions/no-tool recovery/finalization, generic execute no-tool recovery, missing-tool no-tool recovery, unapproved Plan no-tool materialization/recovery, final-turn completion, assistant completion phase, reasoning-dominated no-tool recovery, empty-response no-tool recovery, Unity MCP runtime state/no-tool handler, execution-evidence/tool-activity classification, unapproved Plan read-only convergence/post-convergence redirect, Plan quality recovery, and completion guards have their first independent modules. Remaining Phase 3 work is now less about individual fallback branches and more about collapsing the retained `execute` setup surface into runner-level orchestration; this slice moved Plan runtime phase/quality/evidence/convergence/activity/closure prompt state, Plan review/closure handlers, turn-start preparation, loop-control/progress handlers, runtime intent/tool-surface/Unity fallback wiring, mutable execute/chat/Plan action callbacks, stream-control state, recovery prompt flags, shared no-tool counters, read-loop/repeat-loop guard state, tool-execution caches, completion-evidence/checkpoint state, assistant visible-text/reply-option policy, pseudo-tool recovery, forced web-research routing, initial assistant no-tool recovery routing/side effects, assistant language recovery routing/side effects, assistant output routing/progress presentation side effects, final-text-only tool-call handling, assistant stream post-processing phase orchestration, assistant display/action/no-tool phase orchestration, assistant completion/no-tool phase orchestration, assistant iteration phase orchestration, per-iteration stream request preparation, approved-plan no-tool routing, tool-progress routing, tool-call execution phase orchestration, Unity MCP no-tool recovery side effects, post-tool-result recovery phase orchestration, high-level tool iteration phase orchestration, and loop-level mutable state creation/folding into explicit runtime state objects, side-effect handlers, phase handlers, or pure decision helpers, while bringing the retained method to roughly 501 AST lines.
- Phase 5 completed the Composer ownership slice and the first e2e scenario registry. Broad
  `ChatArea`/`SettingsModal` decomposition is deferred until a specific state owner or maintenance
  hotspot justifies it; file size alone is not a restart condition.

## Verification

- `npm run build`
- `npx tsc --noEmit --pretty false`
- `node --test --test-reporter=dot tests/node/orchestrator-completion-guards.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 runner/completion guard slice)
- `node --test --test-reporter=dot tests/node/tool-activity-tracking.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 tool-activity/evidence slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 tool-surface planning slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/context-trim.test.mjs` (focused Phase 3 context-management slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/tool-capabilities.test.mjs tests/node/mcp-client.test.mjs` (focused Phase 3 tool-registry setup slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/context-trim.test.mjs tests/node/cloud-retry.test.mjs tests/node/provider-compatibility.test.mjs` (focused Phase 3 stream-recovery slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-capabilities.test.mjs` (focused Phase 3 tool-execution round slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-capabilities.test.mjs` (focused Phase 3 tool-call partitioning/tool-result history/tool-execution slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 loop-recovery slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 no-progress loop-recovery slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 cross-iteration read_file loop-recovery slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/repetition-guard.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 target-progress loop-recovery slice)
- `node --test --test-reporter=dot tests/node/plan-execution-recovery.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/repetition-guard.test.mjs` (focused Phase 3 strict repeat guard recovery slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/repetition-guard.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 execute-convergence prompt slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 max-iteration boundary slice)
- `node --test --test-reporter=dot tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 approved-plan recovery actions slice)
- `node --test --test-reporter=dot tests/node/plan-execution-recovery.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 approved-plan no-tool recovery slice)
- `node --test --test-reporter=dot tests/node/plan-execution-recovery.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 approved-plan finalization slice)
- `node --test --test-reporter=dot tests/node/execute-recovery.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 generic execute no-tool recovery slice)
- `node --test --test-reporter=dot tests/node/missing-tool-call-reprompt.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 missing-tool no-tool recovery slice)
- `node --test --test-reporter=dot tests/node/plan-execution-recovery.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 unapproved Plan no-tool recovery slice)
- `node --test --test-reporter=dot tests/node/unity-mcp-fallback.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/tool-capabilities.test.mjs` (focused Phase 3 Unity MCP runtime slice)
- `node --test --test-reporter=dot tests/node/unity-mcp-fallback.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 Unity MCP runtime state slice)
- `node --test --test-reporter=dot tests/node/plan-readonly-convergence.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-runtime-state.test.mjs tests/node/plan-unsupported-tool-call.test.mjs` (focused Phase 3 Plan read-only convergence and post-convergence redirect slice)
- `node --test --test-reporter=dot tests/node/plan-readonly-convergence.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 Plan quality recovery slice)
- `node --test --test-reporter=dot tests/node/tool-activity-tracking.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 tool-result post-processing slice)
- `node --test --test-reporter=dot tests/node/tool-call-execution-phase.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/tool-execution-runtime-state.test.mjs tests/node/execute-recovery.test.mjs` (focused Phase 3 tool-call execution phase slice)
- `node --test --test-reporter=dot tests/node/tool-result-recovery-phase.test.mjs tests/node/tool-call-execution-phase.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/tool-execution-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/plan-readonly-convergence.test.mjs` (focused Phase 3 tool-result recovery phase slice)
- `node --test --test-reporter=dot tests/node/tool-iteration-phase.test.mjs tests/node/tool-call-execution-phase.test.mjs tests/node/tool-result-recovery-phase.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/tool-execution-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-readonly-convergence.test.mjs` (focused Phase 3 tool iteration phase orchestration slice)
- `node --test --test-reporter=dot tests/node/final-turn-completion.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 final-turn completion slice)
- `node --test --test-reporter=dot tests/node/reasoning-no-tool-recovery.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/stream-watchdog.test.mjs tests/node/plan-runtime-state.test.mjs` (focused Phase 3 reasoning-dominated no-tool recovery slice)
- `node --test --test-reporter=dot tests/node/empty-response-recovery.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/stream-watchdog.test.mjs tests/node/missing-tool-call-reprompt.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 empty-response no-tool recovery slice)
- `node --test --test-reporter=dot tests/node/assistant-response-processing.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/normalized-turn.test.mjs` (focused Phase 3 assistant response processing slice)
- `node --test --test-reporter=dot tests/node/turn-iteration-context.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/assistant-response-processing.test.mjs` (focused Phase 3 TurnIterationContext slice)
- `node --test --test-reporter=dot tests/node/turn-iteration-context.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/final-turn-completion.test.mjs` (focused Phase 3 TurnIterationContext propagation slice)
- `node --test --test-reporter=dot tests/node/execute-recovery-runtime.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 execute-recovery runtime state slice)
- `node --test --test-reporter=dot tests/node/approved-plan-recovery-runtime.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/reasoning-no-tool-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 approved-plan recovery runtime state slice)
- `node --test --test-reporter=dot tests/node/plan-runtime-state-loop.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-readonly-convergence.test.mjs tests/node/plan-runtime-state.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/reply-options.test.mjs` (focused Phase 3 Plan runtime state slice)
- `node --test --test-reporter=dot tests/node/stream-runtime-state.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/assistant-response-processing.test.mjs tests/node/stream-watchdog.test.mjs tests/node/reasoning-no-tool-recovery.test.mjs` (focused Phase 3 stream runtime state slice)
- `node --test --test-reporter=dot tests/node/recovery-prompt-runtime-state.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/empty-response-recovery.test.mjs tests/node/missing-tool-call-reprompt.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 recovery prompt runtime state slice)
- `node --test --test-reporter=dot tests/node/no-tool-runtime-state.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/empty-response-recovery.test.mjs tests/node/reasoning-no-tool-recovery.test.mjs tests/node/missing-tool-call-reprompt.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 no-tool runtime state slice)
- `node --test --test-reporter=dot tests/node/loop-guard-runtime-state.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/repetition-guard.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 loop-guard runtime state slice)
- `node --test --test-reporter=dot tests/node/tool-execution-runtime-state.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 tool-execution runtime state slice)
- `node --test --test-reporter=dot tests/node/evidence-runtime-state.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/execute-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/orchestrator-completion-guards.test.mjs` (focused Phase 3 evidence runtime state slice)
- `node --test --test-reporter=dot tests/node/loop-mutable-state.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/loop-runtime-actions.test.mjs tests/node/loop-control-runtime.test.mjs tests/node/plan-review-runtime.test.mjs tests/node/tool-iteration-phase.test.mjs tests/node/assistant-iteration-phase.test.mjs` (focused Phase 3 loop mutable state slice)
- `node --test --test-reporter=dot tests/node/plan-review-runtime.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/empty-response-recovery.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/plan-readonly-convergence.test.mjs tests/node/plan-runtime-state.test.mjs` (focused Phase 3 Plan review/closure runtime slice)
- `node --test --test-reporter=dot tests/node/turn-preparation-runtime.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-review-runtime.test.mjs tests/node/tool-activity-tracking.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/empty-response-recovery.test.mjs` (focused Phase 3 turn-start preparation slice)
- `node --test --test-reporter=dot tests/node/loop-control-runtime.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/plan-review-runtime.test.mjs tests/node/turn-preparation-runtime.test.mjs` (focused Phase 3 loop-control/progress runtime slice)
- `node --test --test-reporter=dot tests/node/assistant-turn-display.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/assistant-response-processing.test.mjs tests/node/reply-options.test.mjs` (focused Phase 3 assistant display/reply-option decision slice)
- `node --test --test-reporter=dot tests/node/assistant-action-routing.test.mjs tests/node/assistant-turn-display.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/web-research-guard.test.mjs tests/node/missing-tool-call-reprompt.test.mjs` (focused Phase 3 assistant action-routing decision slice)
- `node --test --test-reporter=dot tests/node/assistant-display-action-phase.test.mjs tests/node/assistant-turn-display.test.mjs tests/node/assistant-action-routing.test.mjs tests/node/assistant-no-tool-recovery-routing.test.mjs tests/node/assistant-recovery-handling.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/assistant-stream-post-processing-phase.test.mjs tests/node/assistant-completion-phase.test.mjs tests/node/missing-tool-call-reprompt.test.mjs tests/node/final-turn-completion.test.mjs` (focused Phase 3 assistant display/action phase slice)
- `node --test --test-reporter=dot tests/node/assistant-no-tool-recovery-routing.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/assistant-action-routing.test.mjs tests/node/missing-tool-call-reprompt.test.mjs` (focused Phase 3 assistant no-tool recovery route slice)
- `node --test --test-reporter=dot tests/node/assistant-language-recovery-routing.test.mjs tests/node/assistant-no-tool-recovery-routing.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/orchestrator-language-mismatch.test.mjs` (focused Phase 3 assistant language recovery route slice)
- `node --test --test-reporter=dot tests/node/assistant-recovery-handling.test.mjs tests/node/assistant-no-tool-recovery-routing.test.mjs tests/node/assistant-language-recovery-routing.test.mjs tests/node/assistant-output-routing.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused Phase 3 assistant no-tool/language recovery side-effect handling slice)
- `node --test --test-reporter=dot tests/node/approved-plan-no-tool-routing.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 approved-plan no-tool routing slice)
- `node --test --test-reporter=dot tests/node/tool-progress-routing.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-readonly-convergence.test.mjs tests/node/tool-activity-tracking.test.mjs` (focused Phase 3 tool-progress routing slice)
- `node --test --test-reporter=dot tests/node/assistant-output-routing.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/reply-options.test.mjs tests/node/tool-progress-routing.test.mjs` (focused Phase 3 assistant output routing slice)
- `node --test --test-reporter=dot tests/node/assistant-output-phase.test.mjs tests/node/assistant-output-routing.test.mjs tests/node/tool-progress-routing.test.mjs tests/node/assistant-recovery-handling.test.mjs tests/node/assistant-display-action-phase.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/plan-execution-recovery.test.mjs tests/node/plan-readonly-convergence.test.mjs tests/node/final-turn-completion.test.mjs tests/node/tool-call-execution-phase.test.mjs tests/node/tool-result-recovery-phase.test.mjs` (focused Phase 3 assistant output/progress phase slice)
- `node --test --test-reporter=dot tests/node/tool-surface-runtime.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/unity-mcp-fallback.test.mjs tests/node/tool-capabilities.test.mjs tests/node/iteration-stream-preparation.test.mjs` (focused Phase 3 tool-surface runtime slice)
- `node --test --test-reporter=dot tests/node/unity-mcp-fallback.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/tool-capabilities.test.mjs` (focused Phase 3 Unity MCP no-tool runtime handler slice)
- `node --test --test-reporter=dot tests/node/loop-runtime-actions.test.mjs tests/node/tool-surface-runtime.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/execute-recovery-runtime.test.mjs tests/node/stream-runtime-state.test.mjs tests/node/loop-guard-runtime-state.test.mjs tests/node/plan-runtime-state-loop.test.mjs tests/node/loop-control-runtime.test.mjs tests/node/tool-result-recovery-phase.test.mjs` (focused Phase 3 loop runtime actions slice)
- `node --test --test-reporter=dot tests/node/final-text-only-tool-call-handling.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/final-turn-completion.test.mjs tests/node/execute-recovery.test.mjs` (focused Phase 3 final-text-only tool-call handling slice)
- `node --test --test-reporter=dot tests/node/iteration-stream-preparation.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/stream-runtime-state.test.mjs tests/node/execute-recovery-runtime.test.mjs tests/node/execute-recovery.test.mjs` (focused Phase 3 per-iteration stream preparation slice)
- `node --test --test-reporter=dot tests/node/assistant-completion-phase.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/approved-plan-no-tool-routing.test.mjs tests/node/final-turn-completion.test.mjs tests/node/execute-recovery.test.mjs tests/node/missing-tool-call-reprompt.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 assistant completion phase slice)
- `node --test --test-reporter=dot tests/node/assistant-stream-post-processing-phase.test.mjs tests/node/assistant-completion-phase.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/assistant-response-processing.test.mjs tests/node/reasoning-no-tool-recovery.test.mjs tests/node/empty-response-recovery.test.mjs tests/node/final-text-only-tool-call-handling.test.mjs tests/node/final-turn-completion.test.mjs tests/node/execute-recovery.test.mjs tests/node/missing-tool-call-reprompt.test.mjs tests/node/plan-execution-recovery.test.mjs` (focused Phase 3 assistant stream post-processing phase slice)
- `node --test --test-reporter=dot tests/node/assistant-iteration-phase.test.mjs tests/node/workflow-runtime-state.test.mjs tests/node/assistant-stream-post-processing-phase.test.mjs tests/node/assistant-display-action-phase.test.mjs tests/node/assistant-output-phase.test.mjs tests/node/assistant-completion-phase.test.mjs tests/node/unity-mcp-fallback.test.mjs` (focused Phase 3 assistant iteration phase orchestration slice)
- `npm run test:workflow-assets` (1093 node tests)
- `node --test --test-reporter=dot tests/node/*.test.mjs` (1093 node tests)
- `npx playwright test tests/e2e/cloud-tool-protocol.spec.ts tests/e2e/awaiting-choice.spec.ts -g "cloud Responses falls back|reply options pause the turn|approved plan execution no-tool replies|approved plan execution replay|ordinary execute repeated read-only loops"` (5 tests)
- `node --test --test-reporter=dot tests/node/submit-async-workflow-run.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused async workflow-run and structure-guard slice)
- `node --test --test-reporter=dot tests/node/submit-intent-routing.test.mjs tests/node/run-intent.test.mjs tests/node/plan-state-hydration.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused submit intent-routing and structure-guard slice)
- `node --test --test-reporter=dot tests/node/submit-runtime-facade.test.mjs tests/node/submit-plan-state-reset.test.mjs tests/node/submit-send-gate-effects.test.mjs tests/node/submit-turn-draft.test.mjs tests/node/submit-pipeline.test.mjs tests/node/workflow-runtime-state.test.mjs` (focused submit runtime/pre-run/controller/turn-draft/plan-reset/send-gate slice)
- `npx playwright test tests/e2e/cloud-tool-protocol.spec.ts -g "existing .MAIN/plans execution hydrates approved plan and exposes execute tools|approved plan resumes with execute runtime tools while preserving plan turn identity"` (2 tests)
- `npx playwright test tests/e2e/plan-reload-resume.spec.ts` (1 test)
- `npx playwright test tests/e2e/awaiting-choice.spec.ts tests/e2e/streaming-timer.spec.ts` (8 tests)
- `npx playwright test tests/e2e/cloud-tool-protocol.spec.ts -g "plain fix request shows operation approval before execute tools|workspace-external local file reads request approval before ingesting and reading|plan executable reply options are ignored when the same turn has tool calls"` (3 tests)
- `npx playwright test tests/e2e/streaming-timer.spec.ts tests/e2e/game-studio-onboarding.spec.ts tests/e2e/game-studio-plan-shortcuts.spec.ts tests/e2e/game-studio-tool-group-collapse.spec.ts` (16 tests)
- `npx playwright test tests/e2e/cloud-tool-protocol.spec.ts -g "game studio execute reply resumes the source turn with studio workflow tools"`
